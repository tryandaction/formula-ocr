# Phase 7 UI and Browser QA

Date: 2026-09-20
URL: `http://127.0.0.1:4174/`
Browser path: Playwright CLI fallback because the Browser plugin was not available. The bundled shell wrapper failed under WSL because its CRLF line endings produced `pipefail\r`; the same underlying `@playwright/cli` command was invoked directly.

## User-Facing Changes

- Stable runtime error classes map to actionable Chinese messages for model unavailable/load failure, queue full, quota, rate limit, authentication, network, timeout, invalid output/LaTeX, unsupported format, oversized input, and cancellation.
- Local companion service offline state displays `本地服务未启动，请先运行 formula-ocr-engine` instead of raw browser `Failed to fetch`.
- Queue-full outcomes become terminal failed items/tasks rather than remaining in a false busy state.

## Browser Checks

| Check | Result |
|---|---|
| Page identity | pass: title `公式识别（Formula OCR）- 图片转 LaTeX` |
| Meaningful first screen | pass: workbench, controls, uploader, filters, exports rendered |
| Framework overlay/blank page | pass: none |
| Provider interaction | pass: modal completes loading and shows local experimental/offline state |
| Markdown upload | pass: `formulas.md` produces exactly two source formulas; code fence ignored |
| Editing | pass: edited result changes to `需要复核`, shows `人工修改`, preview updates |
| Export controls | pass: Markdown/TeX/JSON enabled for selected source formulas |
| Desktop overflow | pass: `scrollWidth 1251 == innerWidth 1251` |
| Mobile overflow | pass: viewport 390x844, `scrollWidth 375 <= innerWidth 390` |

## Console

No React/Vite/runtime exception or warning was observed. Opening the Provider modal while the companion service was intentionally stopped produced browser network errors for `http://127.0.0.1:8502/health` (`ERR_CONNECTION_REFUSED`). The fetch failure was caught and rendered as the expected offline state. This is an explained external-service probe, not an uncaught application error.

## Screenshots

Screenshots were intentionally stored outside the repository:

- Desktop edited results: `%TEMP%/formula-ocr-qa/.playwright-cli/page-2026-09-20T15-53-05-925Z.png`
- Mobile edited results: `%TEMP%/formula-ocr-qa/.playwright-cli/page-2026-09-20T15-53-35-563Z.png`
- Provider loading state: `%TEMP%/formula-ocr-qa/.playwright-cli/page-2026-09-20T15-51-33-116Z.png`

## Automated Regression

- Frontend: 34 test files, 269/269 tests passed.
- ESLint: passed with no output.
- Production build: passed, 77 modules transformed, Vite build 8.51 s.

## Remaining Risk

- Local formula inference was validated in Python, but browser E2E against a simultaneously running model service was not performed in this phase; contract tests cover that transport.
- PDF visual auto-detection remains disabled because measured detection gates failed.
- Local PDF-to-Markdown capability remains disabled because no audited compatible Pix2Text dependency set was demonstrated.
